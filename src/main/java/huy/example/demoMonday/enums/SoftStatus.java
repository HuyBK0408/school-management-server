package huy.example.demoMonday.enums;

public enum SoftStatus {
    ACTIVE,           // đang hoạt động (mặc định)
    PENDING_DELETE,   // yêu cầu xoá (soft)
    DELETED           // đã xoá mềm (không hiển thị)
}